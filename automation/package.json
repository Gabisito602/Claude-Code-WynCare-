{
  "name": "wyncare-rating-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Worker que automatiza el portal del agente en la aseguradora para tarificar cotizaciones reales.",
  "main": "rating_worker.js",
  "scripts": {
    "start": "node rating_worker.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2",
    "dotenv": "^16",
    "playwright": "^1"
  }
}
